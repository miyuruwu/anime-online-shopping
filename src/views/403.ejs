<%- include('./partials/head', { title }) %>
<%- include('./partials/nav', { cartCount: 0, currentPath }) %>

<main class="container">
  <section class="empty">
    <div class="empty-title"><%= title || "Forbidden" %></div>
    <div class="empty-sub"><%= subtitle || "You don’t have access to this page." %></div>
    <div class="empty-actions">
      <a class="btn btn-primary" href="/shop">Back to shop</a>
    </div>
  </section>
</main>

<%- include('./partials/footer') %>

